def heap_sort(arr):
    # Sorts a list in ascending order using Heap Sort algorithm.
    
    # 1. Builds a min-heap from the input array.
    # 2. Repeatedly extracts the minimum element (root) and places it at the end.
    # 3. Restores the heap property on the reduced heap.
    
    pass